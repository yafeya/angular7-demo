import { SubmitAlertDirective } from './submit-alert.directive';

describe('SubmitAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new SubmitAlertDirective();
    expect(directive).toBeTruthy();
  });
});
